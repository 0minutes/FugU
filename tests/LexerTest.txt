/*
THIS IS NOT THE FUTURE SYNTAX, I JUST COPPIED AND PASTED SOME RANDOM CODE TO MAKE SURE THE LEXER WORKS WELL
*/

let x = 10; // Assign an integer to x
let y = 20.5; // Assign a float to y

const greeting = "Hello, Lexer!"; // Assign a string
const initial = 'La'; // Assign a character

// Control structures
if (x < y && y > 0) {
    x += 1;
    y -= 2;
} else if (x > y || y < 0) {
    x++;
    y--;
} else {
    x = y;
};

while (x <= 50 || y >= 10) {
    x *= 2;
    y /= 3;
}


for (let i = 0; i < 10; i++) {
    print(i);
};

const result = proc add(a, b) {
    return a + b;
};  

let z = add(x, y);

x != y;  // Not equal comparison
z == 42;  // Equality comparison

// Logical and bitwise operators
let condition = true || false && !true;
let bitwise = ~x & y | z ^ 42;
let shifted = x << 2 >> 1;

// Type checking and other operations
let isNumber = typeof x == "number";
let empty = null;

// Include and new keyword usage
include "module";
let obj = new class {
    constructor() {
        this.name = "Object";
    }
};
print("Finished lexer test!");

// Nested control structures
for (let j = 0; j < 5; j++) {
    if (j % 2 == 0) {
        while (j < 3) {
            j++;
        }
    }
}

const finalValue = input("Enter a value: ");